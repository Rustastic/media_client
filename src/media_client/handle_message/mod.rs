use messages::high_level_messages::Message;

use super::MediaClient;

impl MediaClient {
    pub fn handle_message(&self, message: Message) {
        match message.content {
            
        }
    }
}
